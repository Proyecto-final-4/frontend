import { getCurrentAndPreviousMonthRanges } from "@/lib/period"; import { isEmptySummaryPeriod, topExpenseCategories } from "@/lib/summary-widget-helpers"; import { getSummary, getSummaryTrends } from "@/sdk/summary"; import type { CategorySummary, SummaryResponse, SummaryTrendsResponse } from "@/types/summary"; export type DashboardWidgetsData =   | { kind: "empty" }   | {       kind: "data";       summary: SummaryResponse;       trends: SummaryTrendsResponse;       topExpenses: CategorySummary[];     }; export async function loadDashboardWidgetsData(   token: string | null, ): Promise<DashboardWidgetsData> {   if (!token) return { kind: "empty" };   const { current, previous } = getCurrentAndPreviousMonthRanges();   try {     const [summary, trends] = await Promise.all([       getSummary(token, current.from, current.to),       getSummaryTrends(token, {         currentFrom: current.from,         currentTo: current.to,         previousFrom: previous.from,         previousTo: previous.to,       }),     ]);     if (isEmptySummaryPeriod(summary)) return { kind: "empty" };     return {       kind: "data",       summary,       trends,       topExpenses: topExpenseCategories(summary.expenseByCategory),     };   } catch {     return { kind: "empty" };   } }