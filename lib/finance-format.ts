export function formatCurrency(amount: number): string {   return new Intl.NumberFormat("es-AR", {     style: "currency",     currency: "ARS",     minimumFractionDigits: 0,     maximumFractionDigits: 0,   }).format(amount); } export function formatPercent(value: number, fractionDigits = 1): string {   return `${value.toFixed(fractionDigits)}%`; } export function formatDelta(amount: number): string {   return `${amount > 0 ? "+" : ""}${formatCurrency(amount)}`; } export type SavingsRateTone = "positive" | "neutral" | "negative"; export function savingsRateTone(rate: number | null): SavingsRateTone {   if (rate === null || Number.isNaN(rate)) return "neutral";   if (rate > 20) return "positive";   if (rate >= 10) return "neutral";   return "negative"; } export const SAVINGS_RATE_TONE_CLASS: Record<SavingsRateTone, string> = {   positive: "text-emerald-700 bg-emerald-50 border-emerald-200",   neutral: "text-amber-700 bg-amber-50 border-amber-200",   negative: "text-red-700 bg-red-50 border-red-200", };