import type { MonthPeriod } from "@/types/summary"; function toIsoDate(date: Date): string {   const year = date.getFullYear();   const month = String(date.getMonth() + 1).padStart(2, "0");   const day = String(date.getDate()).padStart(2, "0");   return `${year}-${month}-${day}`; } export function getCurrentAndPreviousMonthRanges(now = new Date()) {   const year = now.getFullYear();   const month = now.getMonth();   return {     current: {       from: toIsoDate(new Date(year, month, 1)),       to: toIsoDate(new Date(year, month + 1, 0)),     },     previous: {       from: toIsoDate(new Date(year, month - 1, 1)),       to: toIsoDate(new Date(year, month, 0)),     },   }; }